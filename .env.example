# Local-only development values. Production credentials are generated and
# stored in Google Secret Manager by Terraform.
POSTGRES_DB=vigil
POSTGRES_USER=vigil
POSTGRES_PASSWORD=vigil-local-database
OPERATOR_CREDENTIAL=vigil-local-owner
SESSION_SECRET=local-only-session-secret-change-before-deploying

