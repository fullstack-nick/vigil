apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: vigil-runtime-secrets
spec:
  provider: gke
  parameters:
    secrets: |
      - resourceName: "projects/__PROJECT_NUMBER__/secrets/vigil-db-password/versions/latest"
        path: "db-password"

